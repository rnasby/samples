package carPartsStore;

import org.springframework.boot.SpringApplication;

public class TestCarPartsStoreApplication {
    public static void main(String[] args) {
        SpringApplication.from(Application::main).run(args);
    }
}